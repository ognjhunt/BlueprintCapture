# Capture Raw Contract V3.2 fixture

This compact fixture exercises the contract join between decoded video timing,
one retained ARKit observation, rights scope, and the provider-neutral
downstream candidate registry. It contains no real capture media and proves no
physical sensor behavior or reconstruction qualification.

The candidate manifest digest is SHA-256 over compact, sorted-key JSON after
removing `manifest_digest`, matching the iOS canonicalization rule.
